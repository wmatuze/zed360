{
  "name": "@zed360/config",
  "version": "0.1.0",
  "private": true,
  "files": [
    "typescript"
  ],
  "scripts": {
    "build": "node -e \"console.log('config package has no build output')\"",
    "lint": "node -e \"console.log('config package has no lint targets')\"",
    "typecheck": "node -e \"console.log('config package has no TypeScript sources')\"",
    "test": "node --test"
  }
}
